{
  "name": "findash",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && rm -rf node_modules && npm_config_build_from_source=true npm install",
    "start": "cd backend && node src/app.js"
  }
}
